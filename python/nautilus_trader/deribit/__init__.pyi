# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

import builtins
import datetime
import enum
from nautilus_trader import model
import typing
__all__ = [
    "DeribitCurrency",
    "DeribitDataClientConfig",
    "DeribitDataClientFactory",
    "DeribitEnvironment",
    "DeribitExecClientConfig",
    "DeribitExecutionClientFactory",
    "DeribitHttpClient",
    "DeribitProductType",
    "DeribitUpdateInterval",
    "DeribitWebSocketClient",
    "DeribitWsChannel",
    "py_get_deribit_http_base_url",
    "py_get_deribit_ws_url",
]

@typing.final
class DeribitDataClientConfig:
    r"""
    Configuration for the Deribit data client.
    """
    def py_new(self, product_types: typing.Optional[typing.Sequence[DeribitProductType]], environment: typing.Optional[DeribitEnvironment], api_key: typing.Optional[builtins.str], api_secret: typing.Optional[builtins.str], base_url_http: typing.Optional[builtins.str], base_url_ws: typing.Optional[builtins.str], proxy_url: typing.Optional[builtins.str], http_timeout_secs: typing.Optional[builtins.int], max_retries: typing.Optional[builtins.int], retry_delay_initial_ms: typing.Optional[builtins.int], retry_delay_max_ms: typing.Optional[builtins.int], heartbeat_interval_secs: typing.Optional[builtins.int], update_instruments_interval_mins: typing.Optional[builtins.int], auto_load_missing_instruments: typing.Optional[builtins.bool]) -> DeribitDataClientConfig:
        r"""
        Configuration for the Deribit data client.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class DeribitDataClientFactory:
    r"""
    Factory for creating Deribit data clients.
    """
    def py_new(self) -> DeribitDataClientFactory:
        r"""
        Factory for creating Deribit data clients.
        """
    def py_name(self) -> builtins.str: ...

@typing.final
class DeribitExecClientConfig:
    r"""
    Configuration for the Deribit execution client.
    """
    def py_new(self, trader_id: model.TraderId, account_id: model.AccountId, product_types: typing.Optional[typing.Sequence[DeribitProductType]], environment: typing.Optional[DeribitEnvironment], api_key: typing.Optional[builtins.str], api_secret: typing.Optional[builtins.str], base_url_http: typing.Optional[builtins.str], base_url_ws: typing.Optional[builtins.str], proxy_url: typing.Optional[builtins.str], http_timeout_secs: typing.Optional[builtins.int], max_retries: typing.Optional[builtins.int], retry_delay_initial_ms: typing.Optional[builtins.int], retry_delay_max_ms: typing.Optional[builtins.int]) -> DeribitExecClientConfig:
        r"""
        Configuration for the Deribit execution client.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class DeribitExecutionClientFactory:
    r"""
    Factory for creating Deribit execution clients.
    """
    def py_new(self) -> DeribitExecutionClientFactory:
        r"""
        Factory for creating Deribit execution clients.
        """
    def py_name(self) -> builtins.str: ...

@typing.final
class DeribitHttpClient:
    r"""
    High-level Deribit HTTP client with domain-level abstractions.
    
    This client wraps the raw HTTP client and provides methods that use Nautilus
    domain types. It maintains an instrument cache for efficient lookups.
    """
    def py_new(self, api_key: typing.Optional[builtins.str], api_secret: typing.Optional[builtins.str], base_url: typing.Optional[builtins.str], environment: DeribitEnvironment, timeout_secs: builtins.int, max_retries: builtins.int, retry_delay_ms: builtins.int, retry_delay_max_ms: builtins.int, proxy_url: typing.Optional[builtins.str]) -> DeribitHttpClient:
        r"""
        High-level Deribit HTTP client with domain-level abstractions.
        
        This client wraps the raw HTTP client and provides methods that use Nautilus
        domain types. It maintains an instrument cache for efficient lookups.
        """
    def py_is_testnet(self) -> builtins.bool:
        r"""
        Returns whether this client is connected to testnet.
        """
    def py_is_initialized(self) -> builtins.bool: ...
    def py_cache_instruments(self, instruments: typing.Sequence[typing.Any]) -> None:
        r"""
        Caches instruments for later retrieval.
        """
    def py_cache_instrument(self, instrument: typing.Any) -> None:
        r"""
        # Errors
        
        Returns a Python exception if adding the instrument to the cache fails.
        """
    def py_request_instruments(self, currency: DeribitCurrency, product_type: typing.Optional[DeribitProductType]) -> typing.Any:
        r"""
        Requests instruments for a specific currency.
        """
    def py_request_option_expirations(self, currency: DeribitCurrency) -> typing.Any:
        r"""
        Requests traded option expirations for a settlement currency.
        """
    def py_request_instrument(self, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Requests a specific instrument by its Nautilus instrument ID.
        
        This is a high-level method that fetches the raw instrument data from Deribit
        and converts it to a Nautilus `InstrumentAny` type.
        """
    def py_request_account_state(self, account_id: model.AccountId) -> typing.Any:
        r"""
        Requests account state for all currencies.
        
        Fetches account balance and margin information for all currencies from Deribit
        and converts it to Nautilus `AccountState` event.
        """
    def py_request_trades(self, instrument_id: model.InstrumentId, start: typing.Optional[datetime.datetime], end: typing.Optional[datetime.datetime], limit: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Requests historical trades for an instrument within a time range.
        
        Fetches trade ticks from Deribit and converts them to Nautilus `TradeTick` objects.
        
        # Arguments
        
        * `instrument_id` - The instrument to fetch trades for
        * `start` - Optional start time filter
        * `end` - Optional end time filter
        * `limit` - Optional limit on number of trades (max 1000)
        
        # Pagination
        
        When `limit` is `None`, this function automatically paginates through all available
        trades in the time range using the `has_more` field from the API response.
        When `limit` is specified, pagination stops once that many trades are collected.
        """
    def py_request_bars(self, bar_type: model.BarType, start: typing.Optional[datetime.datetime], end: typing.Optional[datetime.datetime], limit: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Requests historical bars (OHLCV) for an instrument.
        
        Uses the `public/get_tradingview_chart_data` endpoint to fetch candlestick data.
        
        # Supported Resolutions
        
        Deribit supports: 1, 3, 5, 10, 15, 30, 60, 120, 180, 360, 720 minutes, and 1D (daily)
        """
    def py_request_book_snapshot(self, instrument_id: model.InstrumentId, depth: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Requests a snapshot of the order book for an instrument.
        
        Fetches the order book from Deribit and converts it to a Nautilus `OrderBook`.
        
        # Arguments
        
        * `instrument_id` - The instrument to fetch the order book for
        * `depth` - Optional depth limit (valid values: 1, 5, 10, 20, 50, 100, 1000, 10000)
        """
    def py_request_order_status_reports(self, account_id: model.AccountId, instrument_id: typing.Optional[model.InstrumentId], start: typing.Optional[builtins.int], end: typing.Optional[builtins.int], open_only: builtins.bool) -> typing.Any:
        r"""
        Requests order status reports for reconciliation.
        
        Fetches order statuses from Deribit and converts them to Nautilus `OrderStatusReport`.
        
        # Strategy
        - Uses `/private/get_open_orders` for all open orders (single efficient API call)
        - Uses `/private/get_open_orders_by_instrument` when specific instrument is provided
        - For historical orders (when `open_only=false`), iterates over currencies
        """
    def py_request_fill_reports(self, account_id: model.AccountId, instrument_id: typing.Optional[model.InstrumentId], start: typing.Optional[builtins.int], end: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Requests fill reports for reconciliation.
        
        Fetches user trades from Deribit and converts them to Nautilus `FillReport`.
        Automatically paginates through all results using time-cursor advancement.
        
        # Strategy
        - Uses `/private/get_user_trades_by_instrument_and_time` when instrument is provided
        - Otherwise iterates over currencies using `/private/get_user_trades_by_currency_and_time`
        """
    def py_request_position_status_reports(self, account_id: model.AccountId, instrument_id: typing.Optional[model.InstrumentId]) -> typing.Any:
        r"""
        Requests position status reports for reconciliation.
        
        Fetches positions from Deribit and converts them to Nautilus `PositionStatusReport`.
        
        # Strategy
        - Uses `currency=any` to fetch all positions in one call
        - Filters by instrument_id if provided
        """
    def py_request_forward_prices(self, currency: builtins.str, instrument_id: typing.Optional[model.InstrumentId]) -> typing.Any:
        r"""
        Request forward prices for option chain ATM determination.
        
        Single-instrument path (1 HTTP call) if `instrument_id` is provided,
        otherwise bulk path via book summaries.
        """

@typing.final
class DeribitWebSocketClient:
    r"""
    WebSocket client for connecting to Deribit.
    """
    def py_new(self, url: typing.Optional[builtins.str], api_key: typing.Optional[builtins.str], api_secret: typing.Optional[builtins.str], heartbeat_interval: builtins.int, environment: DeribitEnvironment, proxy_url: typing.Optional[builtins.str]) -> DeribitWebSocketClient:
        r"""
        WebSocket client for connecting to Deribit.
        """
    def py_new_public(self, environment: DeribitEnvironment, proxy_url: typing.Optional[builtins.str]) -> DeribitWebSocketClient:
        r"""
        Creates a new public (unauthenticated) client.
        
        Does NOT fall back to environment variables for credentials.
        
        # Errors
        
        Returns an error if initialization fails.
        """
    def py_with_credentials(self, environment: DeribitEnvironment, api_key: typing.Optional[builtins.str], api_secret: typing.Optional[builtins.str], account_id: typing.Optional[model.AccountId], proxy_url: typing.Optional[builtins.str]) -> DeribitWebSocketClient:
        r"""
        Creates an authenticated client with credentials.
        
        Resolves each credential from the provided argument first, falling back
        to the environment variable for the given `environment`:
        - Testnet: `DERIBIT_TESTNET_API_KEY` and `DERIBIT_TESTNET_API_SECRET`
        - Mainnet: `DERIBIT_API_KEY` and `DERIBIT_API_SECRET`
        """
    def py_url(self) -> builtins.str:
        r"""
        Returns the WebSocket URL.
        """
    def py_is_testnet(self) -> builtins.bool: ...
    def py_is_active(self) -> builtins.bool:
        r"""
        Returns whether the client is actively connected.
        """
    def py_is_closed(self) -> builtins.bool:
        r"""
        Returns whether the client is closed.
        """
    def py_has_credentials(self) -> builtins.bool:
        r"""
        Returns whether the client has credentials configured.
        """
    def py_is_authenticated(self) -> builtins.bool:
        r"""
        Returns whether the client is authenticated.
        """
    def py_cancel_all_requests(self) -> None:
        r"""
        Cancel all pending WebSocket requests.
        """
    def py_cache_instruments(self, instruments: typing.Sequence[typing.Any]) -> None:
        r"""
        Caches instruments for use during message parsing.
        """
    def py_cache_instrument(self, instrument: typing.Any) -> None:
        r"""
        Caches a single instrument.
        """
    def py_set_account_id(self, account_id: model.AccountId) -> None:
        r"""
        Sets the account ID for order/fill reports.
        """
    def py_set_bars_timestamp_on_close(self, value: builtins.bool) -> None:
        r"""
        Sets whether bar timestamps should use the close time.
        
        When `true` (default), bar `ts_event` is set to the bar's close time.
        """
    def py_connect(self, loop_: typing.Any, instruments: typing.Sequence[typing.Any], callback: typing.Any) -> typing.Any:
        r"""
        Connects to the Deribit WebSocket API.
        """
    def py_wait_until_active(self, timeout_secs: builtins.float) -> typing.Any:
        r"""
        Waits until the client is active or timeout expires.
        """
    def py_close(self) -> typing.Any:
        r"""
        Closes the WebSocket connection.
        
        # Errors
        
        Returns an error if the close operation fails.
        """
    def py_authenticate(self, session_name: typing.Optional[builtins.str]) -> typing.Any:
        r"""
        Authenticates the WebSocket session with Deribit.
        
        Uses the `client_signature` grant type with HMAC-SHA256 signature.
        This must be called before subscribing to raw data streams.
        
        # Arguments
        
        * `session_name` - Optional session name for session-scoped authentication.
          When provided, uses `session:<name>` scope which allows skipping `access_token`
          in subsequent private requests. When `None`, uses default `connection` scope.
          Recommended to use session scope for order execution compatibility.
        """
    def py_authenticate_session(self, session_name: builtins.str) -> typing.Any:
        r"""
        Authenticates with session scope using the provided session name.
        
        Use `DERIBIT_DATA_SESSION_NAME` for data clients and
        `DERIBIT_EXECUTION_SESSION_NAME` for execution clients.
        """
    def py_subscribe_trades(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to trade updates for an instrument.
        
        # Arguments
        
        * `instrument_id` - The instrument to subscribe to
        * `interval` - Update interval. Defaults to `Ms100` (100ms). `Raw` requires authentication.
        """
    def py_unsubscribe_trades(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from trade updates for an instrument.
        """
    def py_subscribe_book(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval], depth: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Subscribes to order book updates for an instrument.
        
        # Arguments
        
        * `instrument_id` - The instrument to subscribe to
        * `interval` - Update interval. Defaults to `Ms100` (100ms). `Raw` requires authentication.
        """
    def py_unsubscribe_book(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval], depth: typing.Optional[builtins.int]) -> typing.Any:
        r"""
        Unsubscribes from order book updates for an instrument.
        """
    def py_subscribe_book_grouped(self, instrument_id: model.InstrumentId, group: builtins.str, depth: builtins.int, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to grouped (depth-limited) order book updates for an instrument.
        
        Uses the Deribit grouped book channel format: `book.{instrument}.{group}.{depth}.{interval}`
        
        Depth is normalized to Deribit supported values: 1, 10, or 20.
        """
    def py_unsubscribe_book_grouped(self, instrument_id: model.InstrumentId, group: builtins.str, depth: builtins.int, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from grouped (depth-limited) order book updates for an instrument.
        
        Depth is normalized to Deribit supported values: 1, 10, or 20.
        """
    def py_subscribe_ticker(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to ticker updates for an instrument.
        
        # Arguments
        
        * `instrument_id` - The instrument to subscribe to
        * `interval` - Update interval. Defaults to `Ms100` (100ms). `Raw` requires authentication.
        """
    def py_unsubscribe_ticker(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from ticker updates for an instrument.
        """
    def py_subscribe_mark_prices(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to mark prices for the given instrument.
        
        Registers the instrument in the `mark_price_subs` set so the handler
        emits `MarkPriceUpdate` from ticker messages, then subscribes to the ticker channel.
        """
    def py_unsubscribe_mark_prices(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from mark prices for the given instrument.
        
        Removes the instrument from the `mark_price_subs` set and unsubscribes
        from the ticker channel.
        """
    def py_subscribe_index_prices(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to index prices for the given instrument.
        
        Registers the instrument in the `index_price_subs` set so the handler
        emits `IndexPriceUpdate` from ticker messages, then subscribes to the ticker channel.
        """
    def py_unsubscribe_index_prices(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from index prices for the given instrument.
        
        Removes the instrument from the `index_price_subs` set and unsubscribes
        from the ticker channel.
        """
    def py_subscribe_option_greeks(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Subscribes to option greeks for the given instrument.
        
        Registers the instrument in the `option_greeks_subs` set so the handler
        emits `OptionGreeks` from ticker messages, then subscribes to the ticker channel.
        """
    def py_unsubscribe_option_greeks(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any:
        r"""
        Unsubscribes from option greeks for the given instrument.
        
        Removes the instrument from the `option_greeks_subs` set and unsubscribes
        from the ticker channel.
        """
    def py_subscribe_quotes(self, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Subscribes to quote (best bid/ask) updates for an instrument.
        
        Note: Quote channel does not support interval parameter.
        """
    def py_unsubscribe_quotes(self, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Unsubscribes from quote updates for an instrument.
        """
    def py_subscribe_user_orders(self) -> typing.Any:
        r"""
        Subscribes to user order updates for all instruments.
        
        Requires authentication. Subscribes to `user.orders.any.any.raw` channel.
        
        # Errors
        
        Returns an error if client is not authenticated or subscription fails.
        """
    def py_unsubscribe_user_orders(self) -> typing.Any:
        r"""
        Unsubscribes from user order updates for all instruments.
        
        # Errors
        
        Returns an error if unsubscription fails.
        """
    def py_subscribe_user_trades(self) -> typing.Any:
        r"""
        Subscribes to user trade/fill updates for all instruments.
        
        Requires authentication. Subscribes to `user.trades.any.any.raw` channel.
        
        # Errors
        
        Returns an error if client is not authenticated or subscription fails.
        """
    def py_unsubscribe_user_trades(self) -> typing.Any:
        r"""
        Unsubscribes from user trade/fill updates for all instruments.
        
        # Errors
        
        Returns an error if unsubscription fails.
        """
    def py_subscribe_user_portfolio(self) -> typing.Any:
        r"""
        Subscribes to user portfolio updates for all currencies.
        
        Requires authentication. Subscribes to `user.portfolio.any` channel which
        provides real-time account balance and margin updates for all currencies
        (BTC, ETH, USDC, USDT, etc.).
        
        # Errors
        
        Returns an error if client is not authenticated or subscription fails.
        """
    def py_unsubscribe_user_portfolio(self) -> typing.Any:
        r"""
        Unsubscribes from user portfolio updates for all currencies.
        
        # Errors
        
        Returns an error if unsubscription fails.
        """
    def py_subscribe(self, channels: typing.Sequence[builtins.str]) -> typing.Any:
        r"""
        Subscribes to multiple channels at once.
        """
    def py_unsubscribe(self, channels: typing.Sequence[builtins.str]) -> typing.Any:
        r"""
        Unsubscribes from multiple channels at once.
        """
    def py_subscribe_perpetual_interest_rates(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any: ...
    def py_unsubscribe_perpetual_interest_rates(self, instrument_id: model.InstrumentId, interval: typing.Optional[DeribitUpdateInterval]) -> typing.Any: ...
    def py_subscribe_instrument_status(self, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Subscribes to instrument status changes for lifecycle notifications.
        
        Channel format: `instrument.state.{kind}.{currency}`
        """
    def py_unsubscribe_instrument_status(self, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Unsubscribes from instrument status changes.
        """
    def py_subscribe_volatility_index(self, index_name: builtins.str) -> typing.Any:
        r"""
        Subscribes to volatility index updates for the given index name.
        
        Channel format: `deribit_volatility_index.{index_name}`
        """
    def py_unsubscribe_volatility_index(self, index_name: builtins.str) -> typing.Any:
        r"""
        Unsubscribes from volatility index updates for the given index name.
        """
    def py_subscribe_chart(self, instrument_id: model.InstrumentId, resolution: builtins.str) -> typing.Any:
        r"""
        Subscribes to chart/OHLC bar updates for an instrument.
        
        # Arguments
        
        * `instrument_id` - The instrument to subscribe to
        * `resolution` - Bar resolution: "1", "3", "5", "10", "15", "30", "60", "120", "180",
          "360", "720", "1D" (minutes or 1D for daily)
        """
    def py_unsubscribe_chart(self, instrument_id: model.InstrumentId, resolution: builtins.str) -> typing.Any:
        r"""
        Unsubscribes from chart/OHLC bar updates.
        """
    def py_subscribe_bars(self, bar_type: model.BarType) -> typing.Any:
        r"""
        Subscribes to bar updates for an instrument using a BarType specification.
        
        Converts the BarType to the nearest supported Deribit resolution and subscribes
        to the chart channel.
        """
    def py_unsubscribe_bars(self, bar_type: model.BarType) -> typing.Any:
        r"""
        Unsubscribes from bar updates for an instrument using a BarType specification.
        """
    def py_submit_order(self, order_side: model.OrderSide, quantity: model.Quantity, order_type: model.OrderType, client_order_id: model.ClientOrderId, trader_id: model.TraderId, strategy_id: model.StrategyId, instrument_id: model.InstrumentId, price: typing.Optional[model.Price], time_in_force: typing.Optional[model.TimeInForce], post_only: builtins.bool, reduce_only: builtins.bool, trigger_price: typing.Optional[model.Price], trigger_type: typing.Optional[model.TriggerType]) -> typing.Any:
        r"""
        Submits an order to Deribit via WebSocket.
        
        Routes to `private/buy` or `private/sell` JSON-RPC method based on order side.
        Requires authentication (call `authenticate_session()` first).
        """
    def py_modify_order(self, order_id: builtins.str, quantity: model.Quantity, price: model.Price, client_order_id: model.ClientOrderId, trader_id: model.TraderId, strategy_id: model.StrategyId, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Modifies an existing order on Deribit via WebSocket.
        
        The order parameters are sent using the `private/edit` JSON-RPC method.
        Requires authentication (call `authenticate_session()` first).
        """
    def py_cancel_order(self, order_id: builtins.str, client_order_id: model.ClientOrderId, trader_id: model.TraderId, strategy_id: model.StrategyId, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Cancels an existing order on Deribit via WebSocket.
        
        The order is cancelled using the `private/cancel` JSON-RPC method.
        Requires authentication (call `authenticate_session()` first).
        """
    def py_cancel_all_orders(self, instrument_id: model.InstrumentId, order_type: typing.Optional[builtins.str]) -> typing.Any:
        r"""
        Cancels all orders for a specific instrument on Deribit via WebSocket.
        
        Uses the `private/cancel_all_by_instrument` JSON-RPC method.
        Requires authentication (call `authenticate_session()` first).
        """
    def py_query_order(self, order_id: builtins.str, client_order_id: model.ClientOrderId, trader_id: model.TraderId, strategy_id: model.StrategyId, instrument_id: model.InstrumentId) -> typing.Any:
        r"""
        Queries the state of an order on Deribit via WebSocket.
        
        Uses the `private/get_order_state` JSON-RPC method.
        Requires authentication (call `authenticate_session()` first).
        """

@typing.final
class DeribitCurrency(enum.Enum):
    r"""
    Deribit currency.
    """
    BTC = ...
    r"""
    Bitcoin
    """
    ETH = ...
    r"""
    Ethereum
    """
    USDC = ...
    r"""
    USD Coin
    """
    USDT = ...
    r"""
    Tether
    """
    EURR = ...
    r"""
    Euro stablecoin
    """
    ANY = ...
    r"""
    All currencies
    """

    def py_new(self, value: typing.Any) -> DeribitCurrency:
        r"""
        Deribit currency.
        """
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def name(self) -> builtins.str: ...
    def value(self) -> builtins.int: ...
    def variants(self) -> builtins.list[builtins.str]: ...
    def py_from_str(self, data: typing.Any) -> DeribitCurrency: ...

@typing.final
class DeribitEnvironment(enum.Enum):
    r"""
    Deribit API environment.
    """
    Mainnet = ...
    r"""
    Live trading environment.
    """
    Testnet = ...
    r"""
    Testnet environment.
    """

    def py_new(self, value: typing.Any) -> DeribitEnvironment:
        r"""
        Deribit API environment.
        """
    def __hash__(self) -> builtins.int: ...
    def __str__(self) -> builtins.str: ...
    def name(self) -> builtins.str: ...
    def value(self) -> builtins.int: ...
    def variants(self) -> builtins.list[builtins.str]: ...
    def py_from_str(self, data: typing.Any) -> DeribitEnvironment: ...

@typing.final
class DeribitProductType(enum.Enum):
    r"""
    Deribit product type.
    """
    Future = ...
    r"""
    Future contract
    """
    Option = ...
    r"""
    Option contract
    """
    Spot = ...
    r"""
    Spot market
    """
    FutureCombo = ...
    r"""
    Future combo
    """
    OptionCombo = ...
    r"""
    Option combo
    """

    def py_new(self, value: typing.Any) -> DeribitProductType:
        r"""
        Deribit product type.
        """
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def name(self) -> builtins.str: ...
    def value(self) -> builtins.int: ...
    def variants(self) -> builtins.list[builtins.str]: ...
    def py_from_str(self, data: typing.Any) -> DeribitProductType: ...

@typing.final
class DeribitUpdateInterval(enum.Enum):
    r"""
    Deribit data stream update intervals.
    
    Controls how frequently updates are sent for subscribed channels.
    Raw updates require authentication while aggregated updates are public.
    """
    Raw = ...
    r"""
    Raw updates - immediate delivery of each event.
    Requires authentication.
    """
    Ms100 = ...
    r"""
    Aggregated updates every 100 milliseconds (default).
    """
    Agg2 = ...
    r"""
    Aggregated updates every 2 ticks.
    """

    def py_new(self, value: typing.Any) -> DeribitUpdateInterval:
        r"""
        Deribit data stream update intervals.
        
        Controls how frequently updates are sent for subscribed channels.
        Raw updates require authentication while aggregated updates are public.
        """
    def __hash__(self) -> builtins.int: ...
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def name(self) -> builtins.str: ...
    def value(self) -> builtins.int: ...
    def variants(self) -> builtins.list[builtins.str]: ...
    def py_from_str(self, data: typing.Any) -> DeribitUpdateInterval: ...

@typing.final
class DeribitWsChannel(enum.Enum):
    r"""
    Deribit WebSocket public data channels.
    
    Channels follow the format: `{channel_type}.{instrument_or_currency}.{interval}`
    """
    Trades = ...
    r"""
    Raw trade stream: `trades.{instrument}.raw`
    """
    Book = ...
    r"""
    Order book updates: `book.{instrument}.{group}.{depth}.{interval}`
    """
    Ticker = ...
    r"""
    Ticker updates: `ticker.{instrument}.{interval}`
    """
    Quote = ...
    r"""
    Quote updates (best bid/ask): `quote.{instrument}`
    """
    PriceIndex = ...
    r"""
    Index price: `deribit_price_index.{currency}`
    """
    PriceRanking = ...
    r"""
    Price ranking: `deribit_price_ranking.{currency}`
    """
    VolatilityIndex = ...
    r"""
    Volatility index: `deribit_volatility_index.{currency}`
    """
    EstimatedExpirationPrice = ...
    r"""
    Estimated expiration price: `estimated_expiration_price.{currency}`
    """
    Perpetual = ...
    r"""
    Perpetual interest rate: `perpetual.{instrument}.{interval}`
    """
    MarkPriceOptions = ...
    r"""
    Mark price options: `markprice.options.{currency}`
    """
    PlatformState = ...
    r"""
    Platform state: `platform_state`
    """
    Announcements = ...
    r"""
    Announcements: `announcements`
    """
    ChartTrades = ...
    r"""
    Chart trades: `chart.trades.{instrument}.{resolution}`
    """
    InstrumentState = ...
    r"""
    Instrument state changes: `instrument.state.{kind}.{currency}`
    Used for instrument lifecycle notifications (created, started, settled, closed, terminated)
    """
    UserOrders = ...
    r"""
    User orders: `user.orders.{instrument}.{interval}`
    """
    UserTrades = ...
    r"""
    User trades/fills: `user.trades.{instrument}.{interval}`
    """
    UserPortfolio = ...
    r"""
    User portfolio: `user.portfolio.{currency}`
    """
    UserChanges = ...
    r"""
    User changes (combined orders/trades/positions): `user.changes.{instrument}.{interval}`
    """
    UserAccessLog = ...
    r"""
    User access log: `user.access_log`
    """

def py_get_deribit_http_base_url(environment: DeribitEnvironment) -> builtins.str: ...

def py_get_deribit_ws_url(environment: DeribitEnvironment) -> builtins.str: ...

